<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.dept.mapper.DeptMapper">
	<select id="selectDeptAll" parameterType="DeptVO"
		resultType="DeptVO">
		SELECT department_id
			,department_name
			,manager_id
			,location_id
		FROM departments
	</select>
	<select id="selectDept" parameterType="DeptVO"
		resultType="DeptVO">
		SELECT department_id
			,department_name
			,manager_id
			,location_id
		FROM departments
		WHERE department_id = #{departmentId}
	</select>
	<insert id="insertDept" parameterType="DeptVO">
		<selectKey keyProperty="departmentId" resultType="Integer"
			order="BEFORE">
			SELECT NVL(MAX(department_id),10)+1
			FROM departments
		</selectKey>
		INSERT INTO departments(
			department_id
			,department_name
			,manager_id
			,location_id
		)
		VALUES(
			#{departmentId}
			,#{departmentName}
			,#{managerId}
			,#{locationId}
		)
	</insert>

	<update id="updateDept" parameterType="DeptVO">
		UPDATE departments
		SET
		department_name = #{dept.departmentName}
		WHERE department_id = #{did}
	</update>

	<delete id="deleteDept" parameterType="int">
		DELETE FROM departments
		WHERE department_id = #{id}
	</delete>
</mapper>